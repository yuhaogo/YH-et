define('node_modules/core-js/library/modules/_is-array-iter', function(require, exports, module) {

  // check on default Array iterator
  var Iterators  = require('node_modules/core-js/library/modules/_iterators')
    , ITERATOR   = require('node_modules/core-js/library/modules/_wks')('iterator')
    , ArrayProto = Array.prototype;
  
  module.exports = function(it){
    return it !== undefined && (Iterators.Array === it || ArrayProto[ITERATOR] === it);
  };

});
