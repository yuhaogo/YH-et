define('node_modules/core-js/library/modules/_create-property', function(require, exports, module) {

  'use strict';
  var $defineProperty = require('node_modules/core-js/library/modules/_object-dp')
    , createDesc      = require('node_modules/core-js/library/modules/_property-desc');
  
  module.exports = function(object, index, value){
    if(index in object)$defineProperty.f(object, index, createDesc(0, value));
    else object[index] = value;
  };

});
