define('node_modules/core-js/library/modules/_keyof', function(require, exports, module) {

  var getKeys   = require('node_modules/core-js/library/modules/_object-keys')
    , toIObject = require('node_modules/core-js/library/modules/_to-iobject');
  module.exports = function(object, el){
    var O      = toIObject(object)
      , keys   = getKeys(O)
      , length = keys.length
      , index  = 0
      , key;
    while(length > index)if(O[key = keys[index++]] === el)return key;
  };

});
