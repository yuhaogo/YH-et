define('node_modules/core-js/library/modules/web.dom.iterable', function(require, exports, module) {

  var global = typeof global !== "undefined" ? global : typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {};
  require('node_modules/core-js/library/modules/es6.array.iterator');
  var global        = require('node_modules/core-js/library/modules/_global')
    , hide          = require('node_modules/core-js/library/modules/_hide')
    , Iterators     = require('node_modules/core-js/library/modules/_iterators')
    , TO_STRING_TAG = require('node_modules/core-js/library/modules/_wks')('toStringTag');
  
  for(var collections = ['NodeList', 'DOMTokenList', 'MediaList', 'StyleSheetList', 'CSSRuleList'], i = 0; i < 5; i++){
    var NAME       = collections[i]
      , Collection = global[NAME]
      , proto      = Collection && Collection.prototype;
    if(proto && !proto[TO_STRING_TAG])hide(proto, TO_STRING_TAG, NAME);
    Iterators[NAME] = Iterators.Array;
  }

});
