define('node_modules/core-js/library/modules/_dom-create', function(require, exports, module) {

  var isObject = require('node_modules/core-js/library/modules/_is-object')
    , document = require('node_modules/core-js/library/modules/_global').document
    // in old IE typeof document.createElement is 'object'
    , is = isObject(document) && isObject(document.createElement);
  module.exports = function(it){
    return is ? document.createElement(it) : {};
  };

});
