define('node_modules/core-js/library/modules/_object-gopd', function(require, exports, module) {

  var pIE            = require('node_modules/core-js/library/modules/_object-pie')
    , createDesc     = require('node_modules/core-js/library/modules/_property-desc')
    , toIObject      = require('node_modules/core-js/library/modules/_to-iobject')
    , toPrimitive    = require('node_modules/core-js/library/modules/_to-primitive')
    , has            = require('node_modules/core-js/library/modules/_has')
    , IE8_DOM_DEFINE = require('node_modules/core-js/library/modules/_ie8-dom-define')
    , gOPD           = Object.getOwnPropertyDescriptor;
  
  exports.f = require('node_modules/core-js/library/modules/_descriptors') ? gOPD : function getOwnPropertyDescriptor(O, P){
    O = toIObject(O);
    P = toPrimitive(P, true);
    if(IE8_DOM_DEFINE)try {
      return gOPD(O, P);
    } catch(e){ /* empty */ }
    if(has(O, P))return createDesc(!pIE.f.call(O, P), O[P]);
  };

});
