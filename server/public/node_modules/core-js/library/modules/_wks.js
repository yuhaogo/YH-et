define('node_modules/core-js/library/modules/_wks', function(require, exports, module) {

  var store      = require('node_modules/core-js/library/modules/_shared')('wks')
    , uid        = require('node_modules/core-js/library/modules/_uid')
    , Symbol     = require('node_modules/core-js/library/modules/_global').Symbol
    , USE_SYMBOL = typeof Symbol == 'function';
  
  var $exports = module.exports = function(name){
    return store[name] || (store[name] =
      USE_SYMBOL && Symbol[name] || (USE_SYMBOL ? Symbol : uid)('Symbol.' + name));
  };
  
  $exports.store = store;

});
