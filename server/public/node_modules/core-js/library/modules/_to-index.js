define('node_modules/core-js/library/modules/_to-index', function(require, exports, module) {

  var toInteger = require('node_modules/core-js/library/modules/_to-integer')
    , max       = Math.max
    , min       = Math.min;
  module.exports = function(index, length){
    index = toInteger(index);
    return index < 0 ? max(index + length, 0) : min(index, length);
  };

});
