define('node_modules/omit.js/index', function(require, exports, module) {

  'use strict';
  
  var assign = require('node_modules/object-assign/index');
  
  module.exports = function omit(obj, fields) {
    var copy = assign({}, obj);
    for (var i = 0; i < fields.length; i++) {
      var key = fields[i];
      delete copy[key];
    }
    return copy;
  };
  

});
